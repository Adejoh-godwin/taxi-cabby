import 'package:flutter/material.dart';

class SnackBar {
  Color error = Colors.red;
  Color warning = Colors.yellow;
  Color success = Colors.green;

  showErrorSnack(String? text, BuildContext context) {}
}
